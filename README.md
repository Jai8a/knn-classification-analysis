# Iris Classification Using K-Nearest Neighbors (KNN)

This project implements a K-Nearest Neighbors (KNN) classifier using scikit-learn to classify Iris flower species based on their morphological features.

The model evaluates classification performance for different values of k and analyzes the impact of feature selection on accuracy.
